# CodeCraft Harness — M4 执行计划（planner 欠账补齐 + 打磨）

> 2026-09-04 定稿。范围：v2.2 §7 的 W6~8（M4），主新增 = **planner（选项 A，补 plan.py 欠账）**，
> 次新增 = API 一致性 / lifespan / v2.2 文档同步 / 归档清理。前置事实：M0~M3 全部实测完毕，
> 单测 29/29（本项目 self-check 复跑通过），五条 MVP 底线证据齐全。
>
> **执行进度（2026-09-05 更新）**：B1+B2 已落地（commit cf55aa5，planner 接入 + 韧性，
> 单测 36/36）；B3 已完成（commit 58871fd，A/B 3/3 全绿，**P5 定稿默认关**——短任务
> 规划是下界开销，与 context_compress 同构按需开；正式报告 docs/planner_ab_report_2026-09-05.md）。
> 定稿决策内容一律未改，仅追加执行状态。

## 1. 为什么 M4 先补 planner（自检结论 → 决策）

v2.2 §4 承诺了 `runtime/plan.py（任务拆 DAG…）`，但代码里没有 planner，README 架构树也悄悄删了这行。
这是**计划与实现之间唯一动到叙事的欠账**——JD 关键词"五大核心模块 ① 任务规划器"目前接不上（现 loop 是
单条 agentic 路径，LLM 每步自选工具，无独立规划阶段）。

**设计定稿（选项 A，控制在一个"不加戏"的度）：**

- **两段式：plan phase（一次规划调用）+ execute phase（现有 agentic loop 原样复用）。**
- 计划是**咨询性脚手架，不是硬状态机**——执行仍让 LLM 每步自选工具。诚实口径：
  "我做了 planner，但它是 advisory plan + agentic execution，不是 DAG 硬调度。"
  这样：不重造一个会与已打通的 self-repair 闭环打架的调度器，保住 6/6；又真把"规划器"接上。
- **计划注入初始 user 消息**（goal + 计划摘要），自动随 checkpoint 持久化 →
  **resume 不重规划、不重付规划 token**（与"压缩不损可恢复性"同理，干净）。
- 计划单独落 `plans` 表做审计/回放口径；planning tokens 计入 meter（预算护栏不被规划调用绕过）。
- 开关 `use_plan`，默认值与 A/B 结果绑定（见 §3 B3）。

## 2. 决策表

| # | 决策 | 结论 | 为什么 |
|---|---|---|---|
| P1 | planner 形态 | advisory plan + agentic execution，非硬 DAG 调度 | 保住已 work 的 self-repair；不引 LangGraph（延续 Q5） |
| P2 | 规划输出 | `AgentPlan{objective, steps:[PlanStep{id,intent,files[],verification}]}`，Pydantic 强校验 + 解析重试 ≤2 | 复用 Q11 模式，LLM 输出可结构化校验 |
| P3 | 计划注入 | 序列化进初始 user 消息（goal 之后） | 随 checkpoint 自动持久化，resume 无需特判 |
| P4 | 审计与成本 | 新增 `plans` 表（run_id, plan_json, planning_tokens）；meter 计 planning tokens | resume/trace 可查；护栏不被规划绕过 |
| P5 | 默认开关 | 待 B3 A/B 实测定（倾向：默认开） | 短任务规划是下界开销，长任务才放大（与压缩同构），先实测不拍脑袋 |
| P6 | 范围收敛 | 只补 planner + 收尾项，不做"规划-执行-再规划"自省循环 | M4 时间预算内够用线优先 |

## 3. 文件级改动

| 文件 | 改动 |
|---|---|
| `app/runtime/plan.py`（新） | `AgentPlan`/`PlanStep` Pydantic 模型 + `PLAN_PROMPT` + `parse_plan()`（引用 protocol.StepParseError，无 import 环）+ `plan_to_text()` |
| `app/runtime/loop.py` | `__init__` 加 `use_plan: bool`；`run()`：无已有 plan 且 use_plan → 调 planner，plan 注入初始消息，产出即 `save_plan`（原子，防杀在 plan phase）；planning tokens 计入 meter；`resume()` 基本不改（plan 已在 checkpoint messages） |
| `app/store/db.py` | `plans` 表 + `save_plan(run_id, plan_json, planning_tokens)` / `get_plan(run_id)`（新表，不动旧表免迁移） |
| `tests/test_planner.py`（新） | ① FakeDecider 先吐 plan JSON → run 只调 planner 一次、plan 注入 context、执行照旧 ② 坏 plan JSON → 重试 → 兜底不崩 ③ plan 后、execute 前被杀 → resume 不重规划（plans 表仅 1 行）、续跑 done ④ 预算 meter 含 planning tokens 时护栏仍生效 |
| `scripts/run_all.py` / `drive_task.py` | 加 `--plan` / `--no-plan` |
| `scripts/measure_plan.py`（新） | plan vs no-plan A/B（复用 measure_compress 模式：同任务各跑 ≥1 次，对比步数 / token / 全绿） |
| `app/main.py` | 见 §4.1 一致性修复 |
| `app/main.py` | `@app.on_event("startup")` → lifespan |
| `docs/执行计划_v2.2.md` | §4 恢复 `plan.py` 行并注明 advisory 口径；§7 里程碑 W6~8 更新为本文 |

## 4. M4 收尾项（非 planner）

### 4.1 API 一致性（补 M0 半成品）
现状：`POST /api/tasks` 只建 pending run 从不执行；`resume` 端点对 budget_paused run 无法批准续跑
（没暴露 approve_budget），且构造 loop 时不传 token_budget/meter → 经 API 续跑的 run 无护栏，与 CLI 语义不一致。
改：POST /api/tasks 真驱动 loop（或 create+run 分离）；resume 加 `approve_budget: bool` 查询/体参数 + 传 token_budget/meter。

### 4.2 其它打磨
- lifespan 替换 on_event（deprecated）。
- v2.2 同步（README 架构树已删 plan 行 → 以本文口径统一到 v2.2 + README）。
- 归档清理：运行期产物（`data/run_all_*.{json,md}`）与正式报告（`docs/`）分目录，避免双份漂移。

### 4.3 最简 UI（Q10：M4 末有余力才做）— 见文末待你拍板
若要：一页 = 提交任务（选任务包+goal）→ 进度轮询 → trace 列表 → 中断/续跑按钮。后端接 §4.1 修复后的 API。

## 5. 里程碑（约 45h，与 v2.2 时间预算一致；超时先砍 4.3）

| 块 | 内容 | 验收 | 状态 |
|---|---|---|---|
| B1 | planner 骨架：plan.py + loop 接入 + plans 表 + 基础单测 | Fake 冒烟：先规划后执行；单测绿 | ✅ 2026-09-05（cf55aa5） |
| B2 | planner 韧性：坏 plan 重试/兜底 + resume 不重规划 + planning tokens 入预算 | test_planner ④ 通过；单测全绿 | ✅ 2026-09-05（cf55aa5，7 用例） |
| B3 | A/B 实测（plan vs no-plan on T1~T3）→ 定 P5 默认开/关 | 数字④ 如实报告（可能"短任务开销下界"） | ✅ 2026-09-05（58871fd，**P5=默认关**） |
| B4 | API 一致性 + lifespan | curl 走通 create→progress→resume(approve) 全链 | ✅ 2026-09-05（86b2925，T1+小预算真机全链过） |
| B5 | 最简 UI（若做） | 提交→进度→续跑 一页可用 | ⏳（待拍板） |
| B6 | 文档同步 + 归档 + 简历口径（planner 新 bullet）+ 面试预演补充 | v2.2/README 一致；简历能答 planner 追问 | ⏳ |

## 6. 诚实口径（面试与文档统一）

- planner 是 advisory plan，不是 DAG 硬调度；执行仍 agentic、LLM 每步自主选工具。
- T1~T3 是短任务，规划额外成本是**下界**；与压缩同构——长任务才放大价值。A/B 如实报告，不承诺"规划让短任务更快"。
- 不引 LangGraph（延续 Q5 自研浓度叙事）。
- 预算护栏语义：planning tokens 计入预算，护栏不可被规划调用绕过。

## 7. 面试预演补充（对 planner 追问）

- **"planner 是不是只是多了一次 prompt？"** → 是结构化产出（Pydantic 强校验+重试）+ 计划进 checkpoint +
  规划/执行两段分离可单独评测；不是把 plan 塞进 system prompt 完事。
- **"为什么不把计划变成硬调度？"** → 见 P1：保住 self-repair，advisory 才能让执行按实测结果偏离原计划。
- **"规划到底省没省钱？"** → 如实给 B3 数字：短任务下界 + 长任务上界趋势（引 measure_plan / measure_compress_scaling 同构论证）。
